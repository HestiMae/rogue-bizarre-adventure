package game;

/**
 * All purpose enum for terrain types. Originally intended to be used to check what ground actors were allowed on,
 * but also useful in checking reed growth for example.
 */
public enum PassableTerrain
{
    WATER,
    LAND
}
